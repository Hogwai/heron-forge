package {{basePackage}};

import dev.hogwai.platform.spi.data.access.DataAccess;
import dev.hogwai.platform.spi.data.access.DataAccessConfiguration;
import dev.hogwai.platform.spi.data.access.DataAccessFactory;
import dev.hogwai.platform.spi.annotation.HeronService;

/** Starter data-access brick generated by Heron. */
@HeronService(value = DataAccessFactory.class, id = "data.{{projectName}}")
public final class {{className}}DataAccessFactory implements DataAccessFactory {

    /** Creates the data-access factory. */
    public {{className}}DataAccessFactory() {
        // Default constructor
    }

    @Override
    public DataAccess open(DataAccessConfiguration configuration) {
        throw new UnsupportedOperationException("Data access implementation is not configured");
    }
}

# Test-only provider fixture for the registry CLI tests.
# Without a resolvable provider on the test classpath, register would fail with PROVIDER_NOT_FOUND.
dev.hogwai.platform.cli.CliTestOrdersProviderFactory

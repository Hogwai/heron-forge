apiVersion: platform.dev/v1alpha1
kind: Application
metadata:
  name: factory-demo
spec:
  capabilities:
    - id: orders
      type: source
      provider:
        id: acme
        version: 1.2.3
      unknownField: true
